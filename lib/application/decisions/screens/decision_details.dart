import 'package:flutter/cupertino.dart';

class DecisionDetailsScreen extends StatelessWidget {
  const DecisionDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
